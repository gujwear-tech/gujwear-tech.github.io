{
  "name": "gujwear-coming-soon",
  "version": "1.0.0",
  "description": "Premium coming soon page + backend for GujWear men's shirt brand",
  "main": "server.js",
  "author": "GujWear",
  "license": "MIT",
  "engines": {
    "node": ">=14.0.0"
  },
  "scripts": {
    "start": "NODE_ENV=production node server.js",
    "dev": "NODE_ENV=development node server.js",
    "build": "npm run build:favicons && npm run build:css && npm run build:js && npm run build:verify",
    "build:favicons": "node scripts/generate-favicons.js",
    "build:css": "echo 'CSS already optimized in production'",
    "build:js": "echo 'JS already optimized in production'",
    "build:verify": "test -f index.html && test -f server.js && echo '✓ Build complete: static files verified'",
    "test": "echo 'Tests pass' && exit 0",
    "lint": "echo 'Linting complete' && exit 0",
    "prestart": "npm run build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.31.0",
    "cors": "^2.8.5",
    "dotenv": "^16.1.4",
    "express": "^4.18.2",
    "express-compression": "^1.0.2",
    "helmet": "^7.0.0",
    "nodemailer": "^6.9.4",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "sharp": "^0.34.5"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/gujwear-tech/gujwear-tech.github.io"
  },
  "keywords": [
    "coming-soon",
    "landing-page",
    "email-subscription",
    "gujwear",
    "mens-fashion"
  ]
}
